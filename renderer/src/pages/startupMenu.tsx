import React, { ReactElement } from "react";
import { StartupMenu } from "@screens/startup-menu/StartupMenu";

export default function startupMenu(): ReactElement {
  return (
    <>
      <StartupMenu />
    </>
  );
}
